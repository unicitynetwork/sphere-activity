PORT=3001
DATABASE_URL=./data/activities.db
ACTIVITY_API_KEY=              # Auto-generated if empty

# Comma-separated list of origins allowed to POST without API key
# These are trusted frontend deployments (browsers send Origin header)
# Example: https://unicitynetwork.github.io,https://sphere.unicity.network
ALLOWED_ORIGINS=https://unicitynetwork.github.io,https://sphere.unicity.network
